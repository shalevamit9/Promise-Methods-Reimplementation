// import { expect } from 'chai';
// import { echo } from '../../src/async-lib';
// import { delay } from '../../src/async-lib/delay';

// describe('echo module', () => {
//   context('#echo', () => {
//     it('should exist', () => {
//       expect(echo).to.be.a('function');
//       expect(echo).to.be.instanceOf(Function);
//     });

//     it('should return an echo after at least 500ms', async () => {
//       const start = Date.now();
//       const result = await echo('hello', 500);
//       const end = Date.now();
//       expect(result).to.be.equal('hello').and;
//     });
//   });
// });
